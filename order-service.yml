server:
  port: 8083

spring:
  application:
    name: order-service
  datasource:
    url: jdbc:mariadb://localhost:3344/microservices
    username: root
    password: ityug
    driver-class-name: org.mariadb.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MariaDBDialect

